fn comptime_char(value, code)
     new (@.t_operand) {
          :code           <= code
          :possible_types <= [set "char"]
          :comptime_val   <= new (@.t_comptime_val) {
               :type  <= "char"
               :value <= value
          }
     }
