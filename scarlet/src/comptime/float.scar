fn comptime_float(value, code)
     new (@.t_operand) {
          :code           <= code
          :possible_types <= [set "float"]
          :comptime_val   <= new (@.t_comptime_val) {
               :type  <= "float"
               :value <= value
          }
     }
