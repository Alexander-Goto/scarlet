fn comptime_byte(value, code)
     new (@.t_operand) {
          :code           <= code
          :possible_types <= [set "byte"]
          :comptime_val   <= new (@.t_comptime_val) {
               :type  <= "byte"
               :value <= value
          }
     }
