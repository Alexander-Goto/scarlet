fn comptime_int(value, code)
     new (@.t_operand) {
          :code           <= code
          :possible_types <= [set "int"]
          :comptime_val   <= new (@.t_comptime_val) {
               :type  <= "int"
               :value <= value
          }
     }
