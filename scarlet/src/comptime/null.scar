let comptime_null_val = new (@.t_comptime_val):type <= "null"

fn comptime_null(code)
     new (@.t_operand) {
          :code           <= code
          :ir             <= "null"
          :possible_types <= [set "null"]
          :comptime_val   <= @.comptime_null_val
     }
