fn comptime_token(value, code)
     new (@.t_operand) {
          :code           <= code
          :possible_types <= [set "token"]
          :comptime_val   <= new (@.t_comptime_val) {
               :type  <= "token"
               :value <= value
          }
     }
