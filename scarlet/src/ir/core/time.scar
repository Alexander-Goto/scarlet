fn ir__core_fn_time(exp, exp_data, ir, final_data, dependencies, src_gconsts, is_last) {
     let code = new (exp:name):text => _ ++ "()"
     ir__fn_call("McoreFNtime", [array], code, [set "time", "error"], exp_data, ir, final_data, dependencies, src_gconsts, is_last)
}