fn ir__core_fn_rnd(exp, exp_data, ir, final_data, dependencies, src_gconsts, is_last) {
     let code = new (exp:name):text => _ ++ "()"

     ir__fn_call("McoreFNrnd", [array], code, [set "int"], exp_data, ir, final_data, dependencies, src_gconsts, is_last)
}