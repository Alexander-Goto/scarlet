fn ir__core_fn_is_debug(exp, exp_data, ir, final_data, dependencies, src_gconsts, is_last) {
     let code = new (exp:name):text => _ ++ "()"

     ir__fn_call("McoreFNdebugB", [array], code, [set "bool"], exp_data, ir, final_data, dependencies, src_gconsts, is_last)
}